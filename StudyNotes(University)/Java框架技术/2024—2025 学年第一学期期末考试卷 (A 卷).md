# 天津师范大学考试试卷

**2024—2025 学年第一学期期末考试卷 (A 卷)**

**科目：Java 框架技术  学院：软件学院  专业：软件工程**

---

### 一、单项选择题
**在每小题的备选答案中选出一个正确答案，并将正确答案的代码填在题干上的括号内。(每小题 1 分，本大题共 20 分)**

1. 在 Spring MVC 的配置文件中，定义控制器搜索路径的标记是（ ）。
   A. `<tx:annonation-driven>`
   B. `<bean if="multipart">`
   C. `<bean id="viewResolver">`
   D. `<context:component-scan>`

2. 如果 Spring MVC 的控制器相应 JSON 格式数据需要在控制器前添加（ ）注解。
   A. @ResponseBody
   B. @Gson
   C. @JsonFormat
   D. @Jackson

3. 在请求的过程中，Spring MVC 使用接口（ ）的实现类完成数据类型转换。
   A. Conversion
   B. SimpleDateFormat
   C. ConversionService
   D. Converter

4. Spring MVC 框架中，注解@RequestMapping()主要用来定义控制器的请求名称，该标记可以放置的位置是（ ）。
   A. 只能在方法前
   B. 只能在属性前
   C. 可以在方法和类前
   D. 可以在方法和属性前

5. 下面关于 Spring MVC 描述错误的是（ ）。
   A. DispatcherServlet 在 Spring MVC 中是核心 servlet，它负责接收请求并将请求分发给适合的控制器
   B. 在 Spring MVC 中，可以配置多个 DispatcherServlet
   C. 若要使 Spring NVC 可用，DispatcherServlet 类需要在 web.xml 中配置
   D. 以上都正确

6. 关于 Spring MVC，以下说法错误的是（ ）。
   A. Spring MVC 的核心入口是 DispatcherServlet
   B. @RequestMapping 注解既可以用在类上也可以用在方法上
   C. @PathVariable 作用是取出 url 中的模板变量作为参数
   D. controller 默认是单例，通过@Scope("prototype")注解改为多例，成员变量共享

7. 下面关于@RequestMapping 注解说法错误的是（ ）。
   A. @RequestMapping 注解的默认属性是 value
   B. @RequestMapping 注解的 value 属性值可以省略
   C. @RequestMapping 注的 value 属性名可以省略解
   D. @RequestMapping 注解的 value 属性必须标注

8. 下列关于 Spring 自动装配的说法中，错误的是（ ）。
   A. 在 Spring 配置文件中，可以通过<bean>元素的 autowire 属性指定自动装配方式
   B. autowire 属性值可以设置为 none、byType、byName
   C. autowire 的属性值之一 byType 表示根据属性类型自动装配
   D. 通过<beans>元素的 default-autowire 属性，可以设置全局的自动装配方式

9. Spring 各模块之间关系是（ ）。
   A. Spring 各模块之间是紧密联系的，互相依赖的
   B. Spring 各模块之间耦合度很高
   C. Spring 的核心模块是必须的，其他模块是基于核心模块
   D. Spring 的模块无法提供定制的架构体系

10. 以下关于 Spring 核心说法不正确的是（ ）。
    A. Spring 核心包括 DI
    B. Spring 核心包括 AOP
    C. Spring 核心包括 IOC
    D. Spring 核心包括 OOP

11. 下面哪一种不是 spring 中的注解（ ）。
    A. service
    B. controller
    C. repository
    D. dao

12. 以下说法不正确的是（ ）。
    A. Spring 是一个轻量级框架，大大简化了 java 企业级开发
    B. Spring 是企业级应用开发的一站式选择，可以取代其他已有框架
    C. Spring 框架最基本的部分是 Spring Core 模块，提供 Ioc 和依赖注入
    D. Spring AOP 是基于 Spring Core 的符合规范的面向切面编程的实现

13. 下面关于在 Spring 中配置 Bean 的 id 属性的说法正确的是（ ）。
    A. id 属性是指 Bean 的实现类
    B. id 属性是必须的，不可以没有
    C. id 属性必须是数字
    D. id 属性的值不可以重复

14. Controller 注解一般用于三层架构中的哪一层（ ）。
    A. 控制层
    B. 业务层
    C. 数据层
    D. 试图层

15. 在 Spring 应用中，由于项目规模变大，Spring 配置文件的规模也会相应增长，这就造成配置文件的可读性和可维护性变差，此时就需要对其进行拆分。以下选项错误的是（ ）。
    A. Spring 配置文件的拆分可以在 web.xml 中实现
    ```xml
    <context-param>
       <param-name>contextConfigLocation</param-name>
       <param-value>
       classpath:applicationContext.xml,classpath:applicationContext-dao.c,l,...略
       </param-value>
    </context-param>
    ```
    B. Spring 配置文件的拆分可以在 web.xml 中实现
    ```xml
    <context-param>
       <param-name>contextConfigLocation</param-name>
       <param-value>
       classpath:applicationContext-*.xml</param-value>
    </context-param>
    ```
    C. Spring 配置文件本身也可以通过 import 子元素导入其他配置文件
    ```xml
    <import resource="applicationContext-dao.xml"/>
    <import resource="applicationContext-service.sml"/>
    .....略
    ```
    D. Spring 配置文件本身也可以通过 import 子元素导入其他配置文件
    ```xml
    <import param="applicationContext-dao.xml"/>
    <import param="applicationContext-service.xml"/>
    ```

16. 分析下面的 Spring 配置代码，可以推断下列语句的运行结果是（ ）。
    Java 语句如下：
    `ApplicationContext context=new ClassPathXmlApplicationContext("di.xml");`
    `System.out.pringln(((B)context.getBean("b")).getM());`
    Spring 配置代码如下：
    ```xml
    <bean id="a" class="A">
        <property name="m" value="1"/>
    </bean>
    <bean id="b" class="B" parent="a">
        <property name="n" value="2"/>
    </bean>
    ```
    A. 编译错误
    B. 什么也不输出
    C. 输出：1
    D. 输出：2

17. 以下关于 Spring 自动装配与依赖检查的说法中错误的是（ ）。
    A. 可以通过 bean 元素的 dependency-check 属性设置依赖检查
    B. 可以通过 bean 元素的 autowire 属性设置自动装配
    C. 可以通过 beans 元素的 autowire 属性设置全局自动装配
    D. 自动装配减少了配置的工作量，但也降低了依赖关系的清晰性和透明性

18. 有如下代码：
    ```xml
    <bean id="userTable" class="com.test.bean.UserTable">
       <property name="userName">
          <value>zhangsan</value>
       </property>
    </bean>
    ```
    关于如上代码，以下说法正确的是（ ）。
    A. 其中`<property name="userName">`的 userName 是 UserTable 中的属性值，可以不要 get、set 方法。
    B. 其中`<property name="userName">`的 userName 是 UserTable 中的属性，可以不要 get 方法，但是一定要有 set 方法
    C. 其中`<property name="userName">`的 userName 是 UserTable 中的属性，可以不要 set 方法，但是一定要有 get 方法
    D. 其中`<property name="userName">`的 userName 是 UserTable 中的属性，一定要有 get 和 set 方法

19. 在哪里设置 Maven 远程仓库的地址（ ）。
    A. config.xml
    B. web.xml
    C. settings.xml
    D. applicationContext.xml

20. Maven 项目的核心文件是 pom.xml，其中 pom 是什么意思（ ）。
    A. Project Object Manage
    B. Project Object Model
    C. Plain Object Manage
    D. Plain Object Model

---

### 二、多项选择题
**在每小题的备选答案中选出二个或二个以上正确答案，并将正确答案的代码填在题干上的括号内，正确答案未选全或选错，该小题无分。(每小题 2 分，本大题共 20 分)**

1. Spring MVC 中的@RequestMapping 注解，支持的属性包括（ ）。
   A. value
   B. method
   C. request
   D. name
   E. response

2. Spring 框架的 AOP 通知类型有（ ）。
   A. ContextLoaderListener
   B. ServletLoaderListener
   C. ContextLoaderServlet
   D. ActionServlet

3. Spring 框架的 AOP 通知类型有（ ）。
   A. Before 通知
   B. After return 通知
   C. Throws 通知
   D. Around 通知

4. 下列选项关于 Spring 配置 DataSource 的说法正确的是（ ）。
   A. Spring 数据源配置方式比较单一，可配性低
   B. Spring 默认的数据源实现是 DriverManagerDataSource
   C. Spring 使用 PropertyPlaceholderConfigurer 可以引入属性文件，在配置文件中采用${…}的方式引用属性值
   D. Spring 通过 JndiObjectFactoryBean 类引用 JNDI 资源，在 Spring 配置文件中，jndiName 值与服务器上配置的 JNDI 数据源名字一样

5. 在 Spring 中，关于 IOC 的理解不正确的是（ ）。
   A. 控制对象生命周期
   B. 不能控制对象销毁
   C. 一定要用接口实现
   D. 核心是反射技术

6. 通过反射技术我们可以得到（ ）。
   A. 构造器相关信息
   B. 属性相关信息
   C. 方法的修饰符、返回类型、名称、参数列表、返回值
   D. 方法体内容

7. 对下面使用 Spring 依赖注入的代码，说法正确的是（ ）。
   `ApplicationContext context=new ClassPathXmlApplicationContext("abc.xml");`
   `Printer printer = (Printer)context.getBean("printer");`
   A. 配置文件 abc.xml 位于 classpath 目录下
   B. 在 abc.xml 中定义了一个 id 或 name 为 printer 的 bean
   C. context.getBean()返回的是 printer 类型的对象，无需强制转换
   D. abc.xml 被修改后，context 会自动重新加载

8. 下列哪些选项属于 HTTP 协议中 GET 方法和 POST 方法的区别内容（ ）。
   A. 提交数据时，POST 比 GET 安全
   B. 提交数据时，POST 没有数据量的限制
   C. GET 方法请求的页面可以作为书签
   D. POST 方法请求的页面可以使用邮件发送

9. 关于@Resource 配置依赖的描述错误的是（ ）。
   A. @Resource 只可以修饰 setter 方法，不能修饰实例变量
   B. @Resource 的 name 属性可以指定依赖 bean 的 id
   C. @Resource 是默认根据 byType 方式实现依赖注入的
   D. @Resource 与<property.../>元素的 ref 属性具有相同的效果

10. 下列关于 ModelAndView 的说法正确的是（ ）。
    A. 控制器处理方法的返回值若为：ModelAndView，则既可以包含视图信息，也可以包含模型数据信息
    B. 控制器处理方法的返回值若为：ModelAndView，在处理方法的方法体内，除了通过 setViewName()或者 setView()设置视图，还必须通过 addobject 添加模型数据
    C. ModelAndView 的 addboject()方法跟 addAllObjects()方法区别：前者添加一个对象到 model 中，后者是添加一个 Map 对象
    D. ModelAndView.setViewName("welcome");中的 welcome 是逻辑视图名，并非真正的视图对象

---

### 三、填空题
**(每小题 2 分，本大题共 20 分)**

1. RESTful 风格在 HTTP 请求中，使用______、delete、post 和 get 方式分别对应添加、删除、修改和查询到操作。
2. 使用基于注解方式的事务管理需要在使用事务的 Spring Bean 类或者 Bean 类的方法上添加注解______。
3. 简单来说，BeanFactory 就是一个管理 Bean 的工厂，它主要负责初始化各种 Bean，并调用它们的______方法。
4. Spring 容器中的 bean 可以分为 5 个范围：singleton、prototype、request、______、______。
5. 数据库连接池预先同数据库建立一些连接，放在内存中，应用程序需要建立数据库连接时直接到连接池中申请一个就行，常用的数据库连接池有______和______。
6. 在使用 Spring MVC 的项目中，进行服务器端验证，用______注解可以实现正则匹配。
7. Spring 支持的事务管理类型有：______事务管理，______事务管理。
8. Spring MVC 中提供了重定向和转发两种模式，如果要用户访问 /main/user.do，假设方法的返回值是 String，则使用重定向应该写为______；使用转发应写为______。
9. 在 xml 文件配置 bean 时需要借助<bean>标签，其中______属性用于标识该对象的唯一性，______属性用于定义该类的全路径信息。
10. 注解装配在默认情况下是不开启的，为了使用注解装配，我们必须在 Spring 配置文件中配置______元素。

---

### 四、名词解释
**(每小题 2 分，本大题共 10 分)**

1. 什么是 IOC?
2. 什么是 AOP?
3. 什么是 JdbcTemplate?
4. 什么是 MVC?
5. 什么是 Ajax?

---

### 五、简答题
**(每小题 5 分，本大题共 20 分)**

1. Spring Framework 有哪些模块组成?
2. 列举 Spring MVC 框架中的常用注解？（5 个及以上）
3. 解释@RequestBody 注解与@ResopnseBody 注解的区别?
4. 解释@Autowired 注解和@Resource 注解的区别?

---

### 六、程序设计题
**(本大题共 10 分)**

1. 实现 Spring+Spring MVC+Spring JdbcTemplate 整合配置的过程？
   有如下文件（不考虑路径问题）：
   Spring 的核心配置文件 applicationContext.xml
   SpringMVC 核心配置文件 Spring-mvc.xml
   仅需完成完成如下文件的配置：
   完成 web.xml 文件配置：
   
   2) <!--设置编码的过滤器：CharacterEncodingFilter--> (3 分)
   
   3) <!--全局的初始化参数：applicationContext.xml--> (2 分)
   
   4) <!--设置-Spring 的监听器：ContextLoaderListener--> (2 分)
   
   5) <!--设置 Spring MVC 前段控制器：DispatcherServlet--> (3 分)